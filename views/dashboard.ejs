<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gradient-to-br from-blue-900 via-zinc-900 to-zinc-800 min-h-screen flex items-center justify-center">
    <div class="bg-white/90 backdrop-blur-md p-10 rounded-3xl shadow-2xl w-full max-w-2xl border border-zinc-200 flex flex-col items-center">
        <svg class="w-20 h-20 text-blue-600 mb-6" fill="none" stroke="currentColor" stroke-width="1.5" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" d="M15.75 6a3.75 3.75 0 11-7.5 0 3.75 3.75 0 017.5 0zM4.5 20.25v-1.5A2.25 2.25 0 016.75 16.5h10.5a2.25 2.25 0 012.25 2.25v1.5" />
        </svg>
        <h1 class="text-4xl font-extrabold text-zinc-800 mb-2 tracking-tight">Welcome to Your Dashboard</h1>
        <p class="text-zinc-600 mb-8 text-center text-lg">You are successfully logged in. Here you can manage your account and explore features.</p>
        
        <% if (typeof user !== 'undefined' && user) { %>
        <div class="mb-8 w-full max-w-md bg-blue-50 rounded-xl p-6 shadow flex flex-col gap-2">
            <div class="flex items-center gap-2">
                <span class="font-semibold text-zinc-700">👤 Username:</span>
                <span class="text-zinc-800"><%= user.username %></span>
            </div>
            <div class="flex items-center gap-2">
                <span class="font-semibold text-zinc-700">📧 Email:</span>
                <span class="text-zinc-800"><%= user.email %></span>
            </div>
            <div class="flex items-center gap-2">
                <span class="font-semibold text-zinc-700">🎂 Age:</span>
                <span class="text-zinc-800"><%= user.age %></span>
            </div>
        </div>
        <% } %>

        <form action="/logout" method="GET" class="w-full max-w-md">
            <button type="submit" class="w-full bg-red-600 text-white font-bold py-3 px-4 rounded-lg hover:bg-red-700 shadow-md hover:shadow-lg transition-all duration-200 text-lg">
                Logout
            </button>
        </form>
    </div>